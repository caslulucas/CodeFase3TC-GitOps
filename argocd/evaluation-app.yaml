apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: evaluation
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/caslulucas/CodeFase3TC-GitOps.git
    targetRevision: HEAD
    path: apps/evaluation

  destination:
    server: https://kubernetes.default.svc
    namespace: evaluation

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

  syncOptions:
  - CreateNamespace=true